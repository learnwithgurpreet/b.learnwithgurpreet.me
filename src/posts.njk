---
layout: layouts/page.njk
title: Archive
eleventyNavigation:
  key: Archive
  order: 2
keywords: tech blog, gurpreet singh, gurpreet from germany, gurpreet from frankfurt, publicissapient, sapient, publicis, advanced javascript tutorials, best free javascript tutorials, best javascript tutorials, best node js tutorials, best tutorials for node js, node js tutorial blog, tutorial javascript, tutorial node js, tutorial node.js, tutorials on javascript, why node.js
excerpt: Blog posts from Gurpreet Singh on technology, sustainablity, and life.
---

<header class="sr-only"><h1>{{ title }}</h1></header>

{% for year, yearPosts in collections.posts | groupByYear | reverse %}
  <h2 class="mb-4 z-[1] mt-12 text-2xl font-extrabold leading-snug tracking-tight md:text-4xl base-color sticky top-0">
    {{ year }}
  </h2>
  <ul>
    {% for post in yearPosts | reverse %}
      <li class="relative">
        <h3 class="text-xl font-extrabold leading-snug tracking md:text-3xl">
          <a href="{{ post.url | url }}" class="hover:underline after:content-[' '] after:absolute after:top-0 after:left-0 after:right-0 after:bottom-0">{{ post.data.title }}</a>
        </h3>
        <time class="mb-6 block text-sm font-normal" datetime="{{ page.date | htmlDateString }}">{{ post.date | readableDate }}</time>
        {# <time class="text-sm text-gray-500" datetime="{{ post.date | date('YYYY-MM-DD') }}">{{ post.date | dateDisplay }}</time> #}
      </li>
    {% endfor %}
  </ul>
{% endfor %}
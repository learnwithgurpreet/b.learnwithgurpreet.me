---
layout: layouts/base.njk
pagination:
  reverse: true
  data: collections
  size: 1
  alias: tag
  filter:
    - all
    - nav
    - post
    - posts
    - tagList
  addAllPagesToCollections: false
eleventyComputed:
  title: Tagged "{{ tag }}"
permalink: /tags/{{ tag | slug }}/
---
<h1 class="title">Tagged <code class="tag {{ metadata.tags[tag] }}">"{{ tag }}"</code></h1>

{% set posts = collections[ tag ] %}
{% include "postlist.njk" %}
